---
exporter:
    namespace: opennebula
    listen_address: :9999
api:
    endpoint: http://one-sandbox:2633/RPC2
    user: oneadmin
    password: opennebula
    insecure_ssl: false
labels:
    - name: region
      match: "(ea|we)st$"
    - name: type
      match: "^(db|web|app)"
