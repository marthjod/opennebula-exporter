---
exporter:
  namespace: opennebula
api:
  endpoint: http://one-sandbox:2633/RPC2
  user: oneadmin
  password: opennebula
  # insecure_ssl: false
labels_vm_name:
  - name: region
    regexp: "(east|west)$"
  - name: type
    regexp: "^(db|web|app).{3}"
labels_user_template:
  - name: custom
    field: CUSTOM_FIELD
